load("@npm//http-server:index.bzl", "http_server")

exports_files(["tsconfig.json", "LICENSE", "package.json", "babel.config.json", "rollup.config.js"], visibility = ["//:__subpackages__"])

http_server(
    name = "serve",
    args = [        
    ],
    data = [
        "//:index.html",
        "//samples/circle-simple:group"
    ]
)