/**
 * @fileoverview
 * Additional externs for JS v3 code samples.
 */

// Panoramio externs reference this, assuming that Closure is being used.
var goog;
goog.math;

/**
 * @interface
 */
goog.math.Size;
